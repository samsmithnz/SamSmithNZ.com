CREATE TABLE [dbo].[valor_guild] (
    [guild_code] INT          NOT NULL,
    [world_num]  INT          NOT NULL,
    [guild_name] VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([guild_code] ASC)
);

